{
  "name": "r8gate-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "R8Gate - distributed rate limiter and API gateway",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "jest",
    "lint": "eslint ."
},
  "devDependencies": {
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "jest": "^30.5.1",
    "prettier": "^3.9.6"
  }
}
